var xmllint = {};

xmllint.validateXML = function (args, files) {
	var Module = {
		arguments: args || [],
		files: files || []
	};

	var output = {
		stdout: '',
		stderr: ''
	};

	/* XMLLINT.RAW.JS */

	return output;
}
